---
import { Picture } from "@astrojs/image/components";
import type { AstroComponentFactory } from "astro/dist/runtime/server";
import { getEmployee } from "../data";

export interface Props {
  email: string;
  tags: string[];
  Content: AstroComponentFactory;
}

const { email, Content, tags } = Astro.props;
const employee = await getEmployee(email);
---

{
  employee && (
    <div class="font-sans h-screen w-full flex flex-row justify-center items-center">
      <div class="card  max-w-96 mx-auto bg-white  shadow-xl hover:shadow">
        <Picture
          src={employee.imageUrl}
          alt={employee.name}
          widths={[200]}
          aspectRatio="1:1"
          formats={["avif", "png"]}
          class="w-32 mx-auto rounded-full -mt-20 border-8 border-white bg-white"
        />
        <div class="text-center mt-2 text-3xl font-medium">{employee.name}</div>
        <div class="text-center mt-2 font-light text-sm">
          <a href={`mailto:${email}`}>{email}</a>
        </div>
        <div class="px-6 mt-2 font-light text-sm">
          <Content />
        </div>
        <hr class="mt-8" />
        <div class="flex p-4">
          {tags.map(tag => (
            <span class="py-2 px-4 shadow-md no-underline text-white rounded-full bg-blue-600 font-sans font-semibold text-sm border-blue mr-2">
              {tag}
            </span>
          ))}
        </div>
      </div>
    </div>
  )
}
